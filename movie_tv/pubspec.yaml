name: movie_tv

description: Android TV offline movie browser

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^8.0.0
  permission_handler: ^11.3.1
  video_thumbnail: ^0.5.3
  path_provider: ^2.1.3

flutter:
  uses-material-design: true